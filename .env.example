# ---------------------------------------------------------------------------
# Public site config — safe to expose to the browser (NEXT_PUBLIC_ prefix).
# Copy this file to ".env.local" and fill in real values.
# ---------------------------------------------------------------------------

# Canonical production URL (used for SEO metadata, sitemap, OG tags).
NEXT_PUBLIC_SITE_URL=https://jigneshraheja.com

# Contact details surfaced in the UI.
NEXT_PUBLIC_EMAIL=jigneshraheja2101@gigzs.com
NEXT_PUBLIC_LINKEDIN_URL=https://www.linkedin.com/in/jignesh-raheja
# WhatsApp number in international format, digits only (no +, spaces or dashes).
NEXT_PUBLIC_WHATSAPP=919999999999
# Calendly scheduling link (optional — leave blank to hide the embed CTA).
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/jignesh-raheja/intro-call
